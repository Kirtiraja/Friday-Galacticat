import { Calc } from './../src/Calc.'

  describe('Calc' , () => {
    test('should correctly create age with user inputted age')
    let userAge = new Calc(38);
    expect(calc.age).toEqual(38);
  });
